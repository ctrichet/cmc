#ifndef EXITCODES_H
#define EXITCODES_H

enum {
    EXIT_OK        = 0,
    EXIT_ERROR     = 1,
    EXIT_BADARGS   = 2,
    EXIT_OUTPUT    = 3,
    EXIT_CLIPBOARD = 4,
    EXIT_MAGIC     = 5,
};

#endif
